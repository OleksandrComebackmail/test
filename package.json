{
  "name": "changelog-sandbox",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "description": "Sandbox to test the changelog flow: PR commits an Unreleased entry after a green build; merge to main cuts a version.",
  "scripts": {
    "build": "node --check src/index.js && echo build ok"
  }
}
